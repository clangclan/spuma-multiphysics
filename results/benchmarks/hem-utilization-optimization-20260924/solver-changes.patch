diff --git a/src/reactiveFoam/ReactiveFoam.C b/src/reactiveFoam/ReactiveFoam.C
index 9ba57a1..0d2b4c3 100644
--- a/src/reactiveFoam/ReactiveFoam.C
+++ b/src/reactiveFoam/ReactiveFoam.C
@@ -240,12 +240,15 @@ public:
         demand(workers>0&&workers<=64&&batch>0,"Invalid thermo worker/batch limits");batchCells=std::min(nc,size_t(batch));
         const bool reuse=dict.getOrDefault<bool>("thermoExactReuse",!mechanical&&!chemistry&&!frozen&&!capillary);
         const word scalarBackend=dict.getOrDefault<word>("closureScalarBackend","cpu");
+        const word fullBackend=dict.getOrDefault<word>("closureBackend","cpu");
         demand(scalarBackend=="cpu"||scalarBackend=="cuda","Unknown closureScalarBackend");
-        demand(!(reuse||scalarBackend=="cuda")||(!mechanical&&!chemistry&&!frozen&&!capillary),
-            "Closure acceleration currently requires nonreacting HEM equilibrium");
+        demand(!reuse||(!mechanical&&!chemistry&&((!frozen&&!capillary)
+            ||(capillary&&fullBackend=="cuda"&&scalarBackend=="cpu"))),
+            "Exact reuse requires nonreacting HEM equilibrium or full CUDA capillary recovery");
+        demand(scalarBackend!="cuda"||(!mechanical&&!chemistry&&!frozen&&!capillary),
+            "Scalar closure acceleration requires nonreacting HEM equilibrium without capillarity");
         fileName scalarLibrary(dict.getOrDefault<fileName>("closureScalarLibrary","libpintleReactiveTransport.so"));scalarLibrary.expand();
         check(pintle_rt_set_closure_acceleration_v1(t,reuse,scalarBackend=="cuda",scalarLibrary.c_str()),"Closure acceleration policy");
-        const word fullBackend=dict.getOrDefault<word>("closureBackend","cpu");
         demand(fullBackend=="cpu"||fullBackend=="cuda","Unknown closureBackend");
         demand(fullBackend!="cuda"||(!mechanical&&!chemistry&&scalarBackend=="cpu"),
             "Full CUDA closure requires nonreacting HEM and closureScalarBackend cpu");
diff --git a/src/reactiveThermo/pintleClosurePool.inc b/src/reactiveThermo/pintleClosurePool.inc
index b3ac708..300e7b8 100644
--- a/src/reactiveThermo/pintleClosurePool.inc
+++ b/src/reactiveThermo/pintleClosurePool.inc
@@ -2,7 +2,9 @@
 // Included inside ThermoPool. Exact, batch-local reuse only.
     void classify(const PintleThermoState* original) {
         ProfileTimer timer(acceleration.classifySeconds);jobCount=0;
-        if(!prototype->exactBatchReuse||operation!=0){
+        const bool reusable=operation==0
+            ||(operation==2&&capillaryColor&&hemDevice);
+        if(!prototype->exactBatchReuse||!reusable){
             for(size_t c=0;c<count;++c){representative[c]=c;order[jobCount++]=c;}return;
         }
         std::fill(hashSlots.begin(),hashSlots.end(),SIZE_MAX);acceleration.eligibleCells+=count;
@@ -16,6 +18,13 @@
                     hash^=word;hash*=1099511628211ULL;bytes+=sizeof(word);n-=sizeof(word);}
                 for(size_t j=0;j<n;++j){hash^=bytes[j];hash*=1099511628211ULL;}};
             add(mass.data()+c*ns,ns*sizeof(double));add(energies+c,sizeof(double));add(original+c,sizeof(*original));
+            // Curved UV recovery also consumes the frozen geometry. Both
+            // values must match bit-for-bit even when bulk input/seed match.
+            // The table is cleared each call; model, operation and capillary
+            // presence are batch-global, so no key survives a policy/stage
+            // change. Include operation explicitly for equilibrium/frozen.
+            add(&operation,sizeof(operation));
+            if(capillaryColor){add(capillaryColor+c,sizeof(double));add(capillaryJump+c,sizeof(double));}
             // Mix high floating-point bits into the low power-of-two index.
             hash^=hash>>33;hash*=0xff51afd7ed558ccdULL;
             hash^=hash>>33;hash*=0xc4ceb9fe1a85ec53ULL;hash^=hash>>33;
@@ -23,7 +32,10 @@
             while(hashSlots[slot]!=SIZE_MAX){const size_t other=hashSlots[slot];
                 if(std::memcmp(mass.data()+c*ns,mass.data()+other*ns,ns*sizeof(double))==0
                     &&std::memcmp(energies+c,energies+other,sizeof(double))==0
-                    &&std::memcmp(original+c,original+other,sizeof(*original))==0){match=other;break;}
+                    &&std::memcmp(original+c,original+other,sizeof(*original))==0
+                    &&(!capillaryColor||(
+                        std::memcmp(capillaryColor+c,capillaryColor+other,sizeof(double))==0
+                        &&std::memcmp(capillaryJump+c,capillaryJump+other,sizeof(double))==0))){match=other;break;}
                 slot=(slot+1)&(hashSlots.size()-1);
             }
             if(match==SIZE_MAX){hashSlots[slot]=c;representative[c]=c;order[jobCount++]=c;}
diff --git a/src/reactiveThermo/pintleReactivePool.inc b/src/reactiveThermo/pintleReactivePool.inc
index 0b7f90b..ad993ec 100644
--- a/src/reactiveThermo/pintleReactivePool.inc
+++ b/src/reactiveThermo/pintleReactivePool.inc
@@ -200,9 +200,9 @@ public:
         if(first<count)throw std::runtime_error("Batch cell "+std::to_string(first)+": "+failures[first].data());
         double maximum=0;if(operation!=0)for(size_t c=0;c<count;++c)maximum=std::max(maximum,drifts[c]);
         for(size_t c=0;c<count;++c) {
-            // Operation-0 recovery consumes immutable conserved mass. Every
-            // other mode retains the staged transactional mass commit.
-            if(operation!=0)std::copy(mass.begin()+c*ns,mass.begin()+(c+1)*ns,q+c*stride);
+            // Both equilibrium and frozen recovery consume immutable mass.
+            // Only reacting operations commit staged chemical mass changes.
+            if(operation%2!=0)std::copy(mass.begin()+c*ns,mass.begin()+(c+1)*ns,q+c*stride);
             output[c]=states[c];}
         *drift=maximum;outcome.success=true;
     }
