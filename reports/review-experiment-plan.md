# 리뷰 후속 실험 계획 — 2026-09-10

검토 대상은 비공개 저장소 `bc900a5` 스냅숏(로컬 `713fc8c`)과 ChatGPT의 「최적화 상태 검토」다. MAIN이 소스 수정, 빌드, 실행 및 결과 판정을 담당한다. 보조 에이전트는 독립적인 읽기 검토만 수행한다.

1. GPU alpha 수송이 실제 필드별 `ddt` 선택까지 Euler인지 검사한다. 기본 backward 및 필드별 backward를 실패 대조군으로 실행한다.
2. 접촉각 경계가 있는 경우 표면장력 캐시를 우회한다. alpha·시각을 고정한 채 U만 바꿔 힘의 변화와 비캐시 결과의 일치를 확인한다.
3. 온도 increment를 적용하기 전에 FP64 실제 잔차를 검증한다. 두 셀 선형계에서 한 번의 고정 sweep은 실패하고 충분한 sweep은 통과해야 한다.
4. 외부 Pintle restart 없이 만드는 작은 세 상 case를 추가한다. Euler 수송, limiter oracle, 위 회귀시험을 실행한다.
5. 버전이 있는 수치 합격 정책을 추가한다. 비유한 U, alpha 범위·합·질량 오차와 출력 비교 실패가 CLI의 비정상 종료로 이어지는지 대조군으로 확인한다. 이는 물리 모델의 인증이 아닌 계산 회귀 기준이다.
6. alpha Sp/Su의 중복 초기화를 없애는 옵션을 동일 바이너리의 on/off로 비교한다. 상 갱신 순서와 FP64 연산은 유지한다. 균형 순서의 12 step 반복과 기존 검증보다 10배 긴 120 step 비교를 분리한다.
7. 장기 비교에 절대 오차, 체적 가중 오차, 상별 질량·계면 지표를 추가한다. 실제 순차 alpha 갱신의 입력과 출력도 별도 진단으로 확인한다. 원래의 동시 최종 상태 dgdt 지표만으로 동등성을 판단하지 않는다.

모든 성능 실행은 전용 실행 잠금 아래 직렬화한다. 원본 case와 바이너리·소스 해시를 보존한다. 원본 `713fc8c` 라이브러리와 실행파일은 별도 보관한다. 신규 최적화의 채택 여부는 측정 뒤 결정하며, 장기 물리 타당성이나 모든 경계조건의 검증을 주장하지 않는다.
