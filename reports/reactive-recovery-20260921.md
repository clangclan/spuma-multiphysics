# ReactiveFoam R04 복원·재시작 개선

현재 상태: 단일 셀/worker 검사 388개, CPU/CUDA 런타임·선택·mechanical·실패 복구 검사 124개 통과(총 512개). 전체 1,042,500셀 G3도 한 프로세스에서 21스텝을 재시도 없이 승인하고 12 ns에서 정상 종료했다. 압축 체크포인트 판정을 보완한 최종 빌드에서도 같은 대형 상태를 재시작하고 한 스텝 더 승인했다. 이는 시작 구간의 수치 인수이며 장시간 분무 정확도 인증은 아니다. 결과는 [summary.json](../results/reactive-recovery-20260921/summary.json)에 분리해 기록한다.

기준 main은 `7d95bd50b5934f41099218d308a57559d2e2f2bd`다. 구현 범위와 뒤에 남는 RF3 작업은 [postmerge v3 안내](../docs/postmerge-v3/README.ko.md)에 있다. 기존 PR #1/selection 결과와 R04 원본 케이스는 수정하지 않았다.

## 원인과 수정

기존 R04 첫 실패 입력 24개와 과거 시험 수정본의 두 번째 스텝 실패 24개를 다시 확보했다. 두 번째 묶음은 기준 복원기가 통과했으며, 액체 분율 차분 폭의 일괄 변경이 회귀를 만든 것으로 확인했다. 기준 탐색을 보존한 첫 개선본은 이 48개를 통과했지만 전체 메시의 다음 입력에서 다시 실패했다.

새 진단으로 RK1 전역 셀 24377(0부터 시작), batch offset 20480/local 3897의 입력을 확보했다. 공기 재고가 액체보다 매우 작은데 이전 액체 질량은 새 종 재고를 초과했다. 고정 `1-1e-8` 분율 seed에서는 인위적으로 큰 IPA 기상량이 만들어져 시작 EOS 기상 branch가 사라졌다. 단순히 dt를 13회 줄여도 같은 탐색 문제가 지속됐다.

`boundaryFallback`은 같은 q/E에서 기상 재고의 로그 좌표, `expm1` 액체량과 직접 계산한 기상 재고를 사용한다. 입력 q/E나 미량 비응축성 재고를 자르지 않는다. 각 물리 후보의 안정성·화학퍼텐셜·음속 검사는 유지하며 경계에서는 기준/보조 탐색의 안정한 해를 엔트로피로 비교한다. 직렬과 pool의 원래 셀 순서, 실패 출력의 원자성도 유지했다.

모든 실패 셀의 요약을 기록하고 batch당 최대 16개의 원래 보존량/seed와 후보 trace를 JSONL로 남긴다. 준비된 [256개 입력](../tests/fixtures/r04-recovery.jsonl)은 전체 메시 없이 재생할 수 있다. 이전 tracer 48개와 새 상세 기록 208개를 보존했으며 중복 셀 입력도 임의 삭제하지 않았다.

## 승인 상태와 체크포인트

전역 보존 검사 전에 GPU attempt를 확정하던 순서를 변경했다. q·열역학 상태·시각·누적 경계 유량은 전역 검사까지 통과해야 승인한다. 반복 실패 시 직전 승인 상태를 저장한다. 실패 상세 파일의 I/O 오류는 수치 timestep 재시도로 처리하지 않는다.

최종 검토에서 GPU 실패 시 Flow 내부와 호출자 양쪽에서 취소하던 경로를 하나의 호출자 소유로 통합했다. 단일 실패는 정확히 한 번, 영구 실패 시험은 13번의 각 시도마다 한 번씩 성공적으로 취소되는지 검사했다. 수정 빌드에서 checkpoint/rollback 18개와 413종 화학 rollback/restart 6개를 다시 통과했다.

새 schema 3은 정확한 이진 q, 열역학 seed, 최초 전역 보존 기준, 누적 경계 유량, 정확한 시각/직전 dt와 누적 스텝·재시도 수를 저장한다. 모든 파일과 complete marker를 숨김 디렉터리에 작성한 뒤 fsync/원자적 rename으로 노출한다. 기존 checkpoint를 교체하는 도중 실패해도 이전 완료 파일은 유지한다. 표식 누락·checksum 불일치·구조 불일치는 거부한다. 기존 schema 1/2 입력 읽기는 유지한다.

## 검증

| 검사 | 결과 |
|---|---|
| 정상 상태 35개 | 기준 결과와 동일 |
| R04 실패 입력 256개 | 모두 복원, 체적/에너지 ≤1e-9, 화학퍼텐셜 ≤1e-7 |
| 공기 0 및 1e-2…1e-14, IPA–공기/N₂O 기체–IPA 액체–공기 | q/E 보존, seed 변동 검사 통과 |
| 평형 음속 독립 방향차분 | 8개, 상대 오차 <1e-4 |
| worker 1/24 | 직렬 결과와 비트 단위 일치 |
| 실패 셀 ID/상세 예산 | 30개 실패의 ID 보존, 상세 16개, 출력 전체 복원 |
| HEM runtime | CPU/CUDA 각각 14개 |
| mechanical runtime | CPU/CUDA 각각 8개 |
| 기존 다중물리 선택/초기화 | 44개 |
| 기존 음향파/413종 화학 rollback | 각각 6개 |
| 새 checkpoint/rollback | 24개: batch/RK2/CUDA/전역수지 실패, 13회 실패 후 보존, 저장 실패, 손상 거부, 20스텝 재시작, 출력 형식·압축 조합 |

ASCII gzip의 `reactiveStateIdentity.gz`도 완료 manifest에서 인정하도록 수정했다. 같은 파일을 수정 전 실행 파일이 거부하는 대조 검사로 문제를 재현했다. 이 SPUMA/OpenFOAM은 binary 출력의 압축을 경고와 함께 해제하므로, binary+압축 요청은 경고·실제 비압축 출력·재시작을 검사한다. ASCII는 압축 없음/gzip을 모두 확인한다. 최초 시험의 잘못된 binary 압축 기대와 종료 시각=시작 시각 입력은 각각 시험 조건을 바로잡았으며, 원래 실패 로그를 보존했다. 물리식이나 잔차 기준은 변경하지 않았다.

재시작 비교는 연속 40스텝과 20+20스텝을 비교했다. 열린 경계 감압에서도 누적 유량을 유지했으며 momentum 경계 적분은 약 `1.6e-4`로 0이 아닌 이력을 검사했다. 그 CPU/CUDA 경계 누적값 차이는 0이고 q 최대 정규화 차이는 `6.80e-15` 이하이다. 작은 주기 경계 재시작의 q 최대 차이는 `1.69e-14` 이하다.

동일 모델 참조와 수치 회귀 결과이며 실험 물성이나 분무 정확도 검증이 아니다. 293.15 K/4 MPa에서 N₂O와 IPA를 모두 액체로 초기화한 고밀도 혼합 상태의 일부 복원 실패는 남아 있다. 새 복원 모드는 비반응 HEM·종 확산 없음에 한정하고 나머지는 명시적으로 거부한다. GPU closure·device chemistry·표면장력·액적 분열·LES·MPI 지원을 추가한 것으로 표시하지 않는다.

## 전체 메시 인수

초기 개선본은 원본과 같은 조건에서 2스텝을 재시도 없이 진행해 이전 두 번째 스텝 실패를 넘었다. 후속 실행은 5스텝을 재시도 없이 통과했다. GPU 실패 취소 수정본의 인수를 위해 완료 체크포인트를 보존하고 런처를 명시적으로 중단했다. 최종 solver/backend를 고정한 새 프로세스에서 그 체크포인트를 읽어 G3를 진행한다. 이전 프로세스의 성공 스텝을 새 프로세스의 20스텝 인수에 합산하지 않는다.

실제 결과는 [전체 메시 검사](../results/reactive-recovery-20260921/full-mesh.json), [종료 상태](../results/reactive-recovery-20260921/full-mesh-status.json), [전체 실행 로그](../results/reactive-recovery-20260921/full-mesh-solver.log)에 있다.

| 전체 메시 항목 | 결과 |
|---|---|
| 셀/worker | 1,042,500 / 단일 프로세스 worker 24개 |
| 이번 프로세스의 연속 승인 | 21스텝, 재시도 0회 |
| 물리 시간 | 2.358660128 ns → 12 ns |
| 통상 dt | 4.717320256e-10 s 유지 |
| 마지막 dt | 종료 시각에 맞춘 2.066993588e-10 s |
| 전역 질량/에너지 최대 오차 | 1.986e-14 / 1.067e-13 |
| 전역 운동량/원소/종 최대 오차 | 7.820e-21 / 1.806e-14 / 1.558e-14 |
| 체적/UV/화학퍼텐셜 최대 잔차 | 1.751e-10 / 9.99992e-10 / 9.98936e-8 |
| 최종 checkpoint | 누적 26스텝, 모든 셀·17개 파일 검사, 음의 종 질량/비유한 값 없음 |
| 관측 최대 RSS/VRAM | 4.343 GB / 1.197 GB (예산 24 GB / 4 GB) |
| GPU 실행/오류 | 1,429 kernel launch, RK stage 42개, failedCalls=0 |
| 비연소 source 호출 | 0회 |

최종 3D 상태의 로컬 위치는 `/home/jsw/문서/analysis/spuma-multiphysics-recovery/logs/r04-final-case/1.2e-08`이며, 원래 전역 보존 기준의 해시를 유지한다. RAM은 `/proc`의 VmHWM, VRAM은 `nvidia-smi`의 프로세스별 값을 5초 간격으로 기록한 관측치다. 종료 직전 표본 사이의 최대값까지 보장하는 값은 아니다.

G3 실행 파일 SHA-256은 `172262b77d243e6fee93c5259ceacb44980874b307cd27e32b22612eb0ba4858`이다. 이후 솔버 변경은 압축 identity 파일명 인정뿐이며, 최종 파일 `1f1f941cd6da24904bb78f511e3a2d56d5f07748287182d630d0833da0a21319`는 저장 형식 포함 24개 검사를 통과했다. 열역학·수송 라이브러리와 계산식은 같다. 최종 빌드는 그 12 ns 상태를 읽고 12.1 ns까지 한 스텝을 재시도 없이 추가 승인했다. 로드 직후 q와 최초 기준·누적 경계 유량은 비트 단위로 같았다. 재복원 p/T 최대 정규화 차이는 각각 `2.585e-9` / `4.327e-10`으로 기존 `1e-8` 기준 이내다. [재시작 비교](../results/reactive-recovery-20260921/restart-parity.json), [최종 빌드 추가 스텝](../results/reactive-recovery-20260921/final-build-continuation.json), [전체 로그](../results/reactive-recovery-20260921/final-build-restart-solver.log)를 보존했다. 빌드별 검증 근거는 [provenance.json](../results/reactive-recovery-20260921/provenance.json)에 분리한다.

## 관측 계산 비용과 후속 범위

21스텝의 시간 중앙값은 149.51 s, p95는 214.00 s, 스텝 시간 합은 3,363.03 s였다. 초기화·저장·종료를 포함한 실행 전체는 3,434.26 s이며, 차이 71.24 s를 초기화와 I/O 각각으로 분리 측정하지는 않았다. 상태 변화에 따라 후반 스텝이 더 느려졌다. worker별 작업 경과 시간은 3,390.13–3,390.70 s, 합계 81,370.40 s이며 이 합계를 실제 실행 시간으로 해석하지 않는다.

이 21스텝 구간의 계산 시간/물리 시간 비율을 단순 환산하면 0.1 µs에 약 9.69시간, 10 µs에 약 40.37일이다. 한 구간의 비용 지표이며 장시간 완료 예측이나 속도 개선율이 아니다. 최적화 OFF/ON의 같은 조건 20스텝×3회 비교는 아직 수행하지 않았다.

P2 exact-state cache, worker/배치 크기 성능 비교와 RF3의 device closure·chemistry·확장 물리는 후속 범위다. 이번 변경은 기존 R04 실패를 넘어 연속 진행과 재시작을 검증한 기반 단계이며 G4 성능 최적화 완료를 주장하지 않는다.
